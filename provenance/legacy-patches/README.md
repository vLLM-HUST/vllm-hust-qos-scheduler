# Legacy patch archive

This directory preserves all original commits from core PR #169 as unmodified
`git format-patch` exports, retaining author metadata and exact diffs.

QoS policy code may be extension-owned. API fields, scheduler insertion, and
metrics wiring require versioned host contracts.
